{
  "name": "tunnelmount",
  "displayName": "TunnelMount",
  "description": "Mount remote filesystems in macOS Finder via VS Code Remote Tunnels",
  "version": "0.0.1",
  "publisher": "twilightcoders",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "extensionKind": [
    "ui"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tunnelmount.mount",
        "title": "TunnelMount: Mount Remote Filesystem in Finder"
      },
      {
        "command": "tunnelmount.unmount",
        "title": "TunnelMount: Unmount Remote Filesystem"
      },
      {
        "command": "tunnelmount.status",
        "title": "TunnelMount: Show Connection Status"
      },
      {
        "command": "tunnelmount.forceActivate",
        "title": "TunnelMount: Force Activate (Debug)"
      }
    ]
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "watch": "npm run compile -- --watch",
    "package": "npm run compile -- --minify",
    "vscode:prepublish": "npm run package"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
